target-version = "py311"
line-length = 140
exclude = [".venv", "build", "dist", "data", "__pycache__"]

[lint]
# Start conservative: pyflakes-only (real bugs like unused imports / undefined names).
# Add more rules later once we're green.
select = ["F"]
