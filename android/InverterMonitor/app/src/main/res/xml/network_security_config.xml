<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Default is HTTPS-only, which works for the Cloudflare tunnel. -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <!-- LAN fallback: allow HTTP to RFC1918 space so a direct local install
         (e.g. http://192.168.x.x:5000) still works. The Flask session cookie is
         marked Secure only when BEHIND_PROXY=1, so LAN-direct users need to
         set BEHIND_PROXY=0 server-side — see CLAUDE.md. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.0.0</domain>
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">172.16.0.0</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
